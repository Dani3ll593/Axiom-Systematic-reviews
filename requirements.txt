# ============================================================================
# AXIOM — requirements.txt (Unificado: Backend + Frontend)
# Optimizado para Google Cloud Compute Engine (CPU) + Featherless API
# ============================================================================

# --- Core UI (Frontend) ---------------------------------------------------
streamlit==1.42.0

# --- Orquestación y Agentes (LangGraph) -----------------------------------
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.9.2
pydantic-settings>=2.4.0

# --- Cliente API (Featherless / OpenAI) -----------------------------------
openai>=1.40.0
instructor>=1.5.0

# --- Embeddings y Vector Store (Local CPU) --------------------------------
FlagEmbedding>=1.2.10
chromadb>=0.5.0
sentence-transformers>=3.0.0
numpy>=2.0,<2.3

# --- Parsing y APIs Científicas -------------------------------------------
pymupdf>=1.24.0
pymed>=0.8.9
arxiv>=2.1.0

# --- Networking & HTTP ----------------------------------------------------
httpx==0.27.2
httpx-sse>=0.4.0
aiohttp>=3.10.0
tenacity==8.5.0

# --- API HTTP wrapper (FastAPI Backend) -----------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# --- Reportes (Generación de PDF) -----------------------------------------
markdown>=3.6
weasyprint>=62.0

# --- Utilidades -----------------------------------------------------------
python-dotenv>=1.0.1
structlog>=24.4.0

# ============================================================================
# ⚠️ ADVERTENCIA SOBRE WEASYPRINT (Generación de PDFs)
# ============================================================================
# Las siguientes dependencias de SISTEMA son requeridas por WeasyPrint.
# Deben instalarse vía apt en el servidor (Ubuntu/Debian), NO con pip:
#
#   sudo apt-get update && sudo apt-get install -y \
#   libpango-1.0-0 \
#   libpangocairo-1.0-0 \
#   libcairo2 \
#   libgdk-pixbuf2.0-0 \
#   shared-mime-info
#
# Sin estas libs, weasyprint importa en Python pero falla al renderizar.
# (Nota: El script setup.sh de Axiom ya incluye esta instalación).
# ============================================================================